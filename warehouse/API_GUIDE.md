# Ozon Seller API - Создание склада и зон доставки Express

## 🚀 Быстрый старт

**Скрипт:** `create_warehouse_full_auto.js`

```javascript
// 1. Откройте https://seller.ozon.ru/app/warehouse
// 2. Вставьте скрипт в консоль (F12 → Console)
// 3. Измените настройки в CONFIG:

const CONFIG = {
    companyId: 2722344,  // ← Ваш ID компании
    warehouseAddress: "188490, Россия, Ленинградская обл, Кингисеппский р-н, г Ивангород, ул Матросова, 2",
    deliveryTimeMinutes: 15,
    courierSpeedKmh: 30
};

// 4. Запустите:
createWarehouseFully()
```

**Скрипт автоматически:**
- 🗺️ Геокодирует адрес → координаты
- 🏭 Создает черновик склада
- 🚚 Создает метод доставки Express
- 📍 Создает зону + полигон доставки (круг по радиусу)
- 🔗 Привязывает и настраивает возвраты
- ✅ Активирует метод доставки

---

## ⚠️ КРИТИЧНО: Обязательные заголовки

**Без этих заголовков API возвращает 403 "Failed to get company ID"!**

### Обязательные заголовки для ВСЕХ запросов к API Ozon Seller:

```http
Content-Type: application/json
Accept: application/json, text/plain, */*
x-o3-app-name: seller-ui
x-o3-language: ru
accept-language: ru
x-o3-company-id: <COMPANY_ID>
x-o3-page-type: warehouse-other
```

### Описание заголовков:

| Заголовок | Значение | Обязательный | Описание |
|-----------|----------|--------------|----------|
| `x-o3-app-name` | `seller-ui` | ✅ Да | Идентификатор приложения |
| `x-o3-language` | `ru` | ✅ Да | Язык интерфейса |
| `x-o3-company-id` | `2721175` | ✅ Да | ID компании продавца |
| `x-o3-page-type` | `warehouse-other` | ⚠️ Рекомендуется | Тип страницы (для склада) |
| `accept-language` | `ru` | ⚠️ Рекомендуется | Язык Accept |
| `Content-Type` | `application/json` | ✅ Да | Формат тела запроса |

### Как получить `x-o3-company-id`:

1. **Из cookies:** `document.cookie` → `sc_company_id=2721175`
2. **Из URL:** `https://seller.ozon.ru/app/...` → параметр `company_id`
3. **Из localStorage:** `localStorage.getItem('sc_company_id')`

### Пример JavaScript:

```javascript
const companyId = document.cookie.match(/sc_company_id=(\d+)/)?.[1] || 'NOT_FOUND';

const response = await fetch('/api/delivery-polygon-service/area/create', {
    method: 'POST',
    credentials: 'include',  // ← Важно для отправки cookies!
    headers: {
        'Content-Type': 'application/json',
        'Accept': 'application/json, text/plain, */*',
        'x-o3-app-name': 'seller-ui',
        'x-o3-language': 'ru',
        'accept-language': 'ru',
        'x-o3-company-id': companyId,
        'x-o3-page-type': 'warehouse-other'
    },
    body: JSON.stringify({ ... })
});
```

---

## Обзор процесса (9 шагов)

| Шаг | Действие | API |
|-----|----------|-----|
| 1 | Геокодирование адреса | `geoproxy.ozon.ru/Suggest` |
| 2 | Создание черновика склада | `/api/site/logistic-service/v3/warehouse/draft/create` |
| 3 | Создание метода доставки | `/api/delivery-method-service/delivery-method/create` |
| 4 | Создание зоны доставки | `/api/delivery-polygon-service/area/create` |
| 5 | Создание полигона | `/api/delivery-polygon-service/v2/polygon/create` |
| 6 | Привязка полигона к зоне | `/api/delivery-polygon-service/area/update` |
| 7 | Привязка склада + возвраты | `/api/delivery-polygon-service/delivery-method/save/warehouse` |
| 8 | **Установка расписания курьера** | `/api/delivery-method-service/delivery-method/courier-schedule/set` |
| 9 | Активация | `/api/delivery-method-service/delivery-method/activate` |

---

## 🔄 WORKFLOW: Создание склада (WarehouseModule)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  • company_id (из cookies: sc_company_id)                               │
│  • warehouseAddress (текстовый адрес)                                   │
│  • deliveryTimeMinutes (время доставки, мин)                            │
│  • courierSpeedKmh (скорость курьера, км/ч)                             │
│  • workingHoursFrom/To (время работы)                                   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 1: ГЕОКОДИРОВАНИЕ                                                  │
│  POST geoproxy.ozon.ru/Suggest                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  warehouseAddress                                               │
│  OUTPUT: lat, lng, parsedAddress                                        │
│                                                                         │
│  Альтернатива: Яндекс Геокодер (если Ozon недоступен)                   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                          lat, lng  │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 2: СОЗДАНИЕ ЧЕРНОВИКА СКЛАДА                                       │
│  POST /api/site/logistic-service/v3/warehouse/draft/create              │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, name, phone, lat, lng, address, timetable          │
│  OUTPUT: warehouseDraftId ────────────────────────────────────┐         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │                           │
                   warehouseDraftId │                           │
                                    ▼                           │
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 3: СОЗДАНИЕ МЕТОДА ДОСТАВКИ                                        │
│  POST /api/delivery-method-service/delivery-method/create               │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, warehouse_draft_id, is_express=true,               │
│          courier_cutoff (время доставки), working_days                  │
│  OUTPUT: methodId ────────────────────────────────────────────┐         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │                           │
                           methodId │                           │
                                    ▼                           │
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 4: СОЗДАНИЕ ЗОНЫ ДОСТАВКИ                                          │
│  POST /api/delivery-polygon-service/area/create                         │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  { area: { delivery_method_id, delivery_time, name } }          │
│  OUTPUT: areaId ──────────────────────────────────────────────┐         │
│                                                               │         │
│  ⚠️ ВАЖНО: body обёрнут в { area: {...} } !                   │         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │                           │
                            areaId  │                           │
                                    ▼                           │
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 5: СОЗДАНИЕ ПОЛИГОНА                                               │
│  POST /api/delivery-polygon-service/v2/polygon/create                   │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  coordinates (JSON строка массива точек круга)                  │
│          Радиус = courierSpeedKmh × deliveryTimeMinutes / 60 × 0.7      │
│  OUTPUT: polygonId ───────────────────────────────────────────┐         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │                           │
                         polygonId  │                           │
                                    ▼                           │
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 6: ПРИВЯЗКА ПОЛИГОНА К ЗОНЕ                                        │
│  POST /api/delivery-polygon-service/area/update                         │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  { area: { id: areaId, multi_polygon_ids: [polygonId] } }       │
│  OUTPUT: success                                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 7: ПРИВЯЗКА СКЛАДА + НАСТРОЙКА ВОЗВРАТОВ                           │
│  POST /api/delivery-polygon-service/delivery-method/save/warehouse      │
│  POST /api/seller-returns-methods/v1/returns-setting                    │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  delivery_method_id, warehouse_id (=draftId), lat, lng          │
│  OUTPUT: success                                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 8: УСТАНОВКА РАСПИСАНИЯ КУРЬЕРА (ОБЯЗАТЕЛЬНО!)                     │
│  POST /api/delivery-method-service/delivery-method/courier-schedule/set │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, delivery_method_id, is_same_as_warehouse=true      │
│  OUTPUT: { result: true }                                               │
│                                                                         │
│  ⚠️ БЕЗ ЭТОГО ШАГА активация вернёт 400:                                │
│     "Для экспресс доставки должно быть указано рабочие расписание"      │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 9: АКТИВАЦИЯ                                                       │
│  POST /api/delivery-method-service/delivery-method/activate             │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, delivery_method_id                                 │
│  OUTPUT: warehouse_id (финальный ID активного склада)                   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЗУЛЬТАТ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  • warehouseId      - ID активного склада                               │
│  • warehouseDraftId - ID черновика                                      │
│  • methodId         - ID метода доставки                                │
│  • areaId           - ID зоны доставки                                  │
│  • polygonId        - ID полигона                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

### Граф зависимостей данных:

```
company_id ──┬──► warehouse/draft/create ──► warehouseDraftId ──┐
             │                                                   │
lat, lng ────┤                                                   ▼
             │                              delivery-method/create ──► methodId
address ─────┘                                                           │
                                                                         │
                                            area/create ◄────────────────┘
                                                  │
                                                  ▼
                                               areaId
                                                  │
lat, lng ──► generateCirclePolygon() ──► coordinates        │
                                               │            │
                                               ▼            ▼
                                        polygon/create ──► polygonId
                                                              │
                                                              ▼
                                                       area/update
                                                              │
warehouseDraftId ──────────────────────────────────────────► │
                                                              ▼
                                               save/warehouse + returns-setting
                                                              │
methodId ─────────────────────────────────────────────────► │
                                                              ▼
                                                       activate
                                                              │
                                                              ▼
                                                        warehouseId
```

---

## 0. Геокодирование адреса

### GET `/api/site/address-service/v2/suggest`

Преобразует текстовый адрес в координаты.

**URL параметры:**
```
?query=188490, Россия, Ленинградская обл...&country=RU&limit=1
```

**Response:**
```json
{
  "suggestions": [
    {
      "value": "188490, Россия, Ленинградская обл...",
      "geo": {
        "lat": 59.376881,
        "lon": 28.212974
      },
      "data": {
        "country": "Россия",
        "region": "Ленинградская",
        "city": "Ивангород",
        "street": "ул Матросова",
        "house": "2",
        "postal_code": "188490"
      }
    }
  ]
}
```

**Fallback:** OpenStreetMap Nominatim API
```
https://nominatim.openstreetmap.org/search?format=json&q={address}&limit=1
```

---

## 1. Создание черновика склада

### POST `/api/site/logistic-service/v3/warehouse/draft/create`

Создает черновик склада Express с адресом, контактами и расписанием.

**Body:**
```json
{
  "company_id": 2722344,
  "name": "Сельпо",
  "phone": "+7 966 879 96 66",
  "warehouse_type": "rfbs_express",
  "address": {
    "address": "Приморский Край, город Уссурийск, Комсомольская улица, 30",
    "longitude": 131.944244,
    "latitude": 43.802948,
    "is_new_address_scheme": true,
    "is_house_missing": false,
    "country": "Россия"
  },
  "timetable_template": {
    "holidays_override": [],
    "working_hours": {
      "1": {"from": "09:00", "to": "21:00"},
      "2": {"from": "09:00", "to": "21:00"},
      "3": {"from": "09:00", "to": "21:00"},
      "4": {"from": "09:00", "to": "21:00"},
      "5": {"from": "09:00", "to": "21:00"},
      "6": {"from": "09:00", "to": "21:00"},
      "7": {"from": "09:00", "to": "21:00"}
    }
  },
  "postings_limit": -1,
  "goods_by_request": false,
  "is_auto_assembly": false
}
```

**Response:**
```json
{
  "result": 4249339
}
```

**Параметры:**
| Поле | Тип | Описание |
|------|-----|----------|
| `company_id` | number | ID компании |
| `name` | string | Название склада |
| `phone` | string | Телефон в формате `+7 XXX XXX XX XX` |
| `warehouse_type` | string | `rfbs_express` для Express |
| `address.address` | string | Полный текстовый адрес |
| `address.latitude` | number | Широта |
| `address.longitude` | number | Долгота |
| `timetable_template.working_hours` | object | Расписание по дням (1=Пн...7=Вс) |
| `postings_limit` | number | Лимит заказов (-1 = без лимита) |

---

## 2. Создание метода доставки Express

### POST `/api/delivery-method-service/delivery-method/create`

Создает метод доставки Express, привязанный к черновику склада.

**Body:**
```json
{
  "company_id": 2722344,
  "delivery_type_id": 1,
  "cutoff": "17:00",
  "name": "Экспресс. Самостоятельно. Сельпо",
  "tariff_type": "STANDARD_OZON",
  "prr_setting": "",
  "tpl_integration_type": "non_integrated",
  "with_item_list": false,
  "make_method_group_id": false,
  "is_express": true,
  "sla_cut_in": 30,
  "courier_cutoff": 15,
  "working_days": [1, 2, 3, 4, 5, 6, 7],
  "warehouse_draft_id": 4249339
}
```

**Response:**
```json
{
  "result": {
    "id": 14650466,
    "warehouse_id": 0,
    "company_id": 2722344,
    "name": "Экспресс. Самостоятельно. Сельпо",
    "delivery_type_id": 1,
    "status": "NEW",
    "is_express": false,
    "sla_cut_in": 30,
    "warehouse_draft_id": 4249339,
    "tpl_integration_type": "non_integrated",
    "tariff_type": "STANDARD_OZON"
  }
}
```

**Параметры:**
| Поле | Тип | Описание |
|------|-----|----------|
| `delivery_type_id` | number | `1` = самостоятельная доставка |
| `cutoff` | string | Крайнее время приёма заказов |
| `tariff_type` | string | `STANDARD_OZON` |
| `tpl_integration_type` | string | `non_integrated` для самостоятельной |
| `is_express` | boolean | `true` для экспресс-доставки |
| `sla_cut_in` | number | Время сборки заказа в минутах |
| `courier_cutoff` | number | Время доставки курьером в минутах |
| `working_days` | array | Рабочие дни [1=Пн...7=Вс] |
| `warehouse_draft_id` | number | ID черновика склада |

---

## 3. Создание зоны доставки
      "country": "Россия",
      "region": "Ленинградская",
      "city": "Ивангород",
      "street": "ул Матросова",
      "house": "2",
      "zipcode": "188490",
      "latitude": 59.376881,
      "longitude": 28.212974,
      "address": "188490, Россия, Ленинградская обл..."
    },
    "phone": "+7 999 123 45 67",
    "workingDays": [1, 2, 3, 4, 5, 6, 7],
    "warehouseType": "rfbs_express"
  }
}
```

**Response:**
```json
{
  "id": "4249339"  // ID черновика склада
}
```

---

## 2. Создание метода доставки

### POST `/api/delivery-method-service/delivery-method/create`

Создает метод доставки Express для черновика.

**Body:**
```json
{
  "company_id": 2722344,
  "warehouse_draft_id": 4249339,
  "delivery_type": "self",
  "is_express": true,
  "name": "Экспресс 15 мин"
}
```

**Response:**
```json
{
  "id": 14650466  // ID метода доставки
}
```

---

## 3. Создание зоны доставки (Area)

### POST `/api/delivery-polygon-service/area/create`

Создает новую зону доставки для метода доставки.

**Body:**
```json
{
  "area": {
    "delivery_method_id": "14650466",  // ID метода доставки (строка)
    "delivery_time": "15",              // Время доставки в минутах (строка)
    "name": "Доставка 15 мин"           // Название зоны
  }
}
```

**Response:**
```json
{
  "id": "957509"  // ID созданной зоны
}
```

---

## 4. Создание полигона (Polygon)

### POST `/api/delivery-polygon-service/v2/polygon/create`

Создает полигон с координатами зоны доставки на карте.

**Body:**
```json
{
  "coordinates": "[[[lat1,lng1],[lat2,lng2],[lat3,lng3],...,[lat1,lng1]]]"
}
```

**Формат координат:**
- JSON-строка с массивом массивов
- Внешний массив: `[[[...]]]` - multipolygon
- Координаты: `[широта, долгота]` (lat, lng) — **широта первая!**
- Полигон должен быть замкнутым (первая и последняя точки совпадают)

### 🔄 Автогенерация неровного полигона

Скрипт автоматически генерирует **неровный** полигон для естественного вида:

```javascript
// Расчет радиуса из времени доставки
radius = (speedKmh * minutes / 60) * 0.7  // коэффициент 0.7 для реальных маршрутов

// Пример: 15 мин @ 30 км/ч = 5.25 км

// Генерация 24 точек с СЛУЧАЙНЫМ отклонением ±15%
for (i = 0; i < 24; i++) {
    angle = (2 * PI * i) / 24;
    
    // Случайный множитель от 0.85 до 1.15 для неровности
    randomFactor = 0.85 + Math.random() * 0.30;
    adjustedRadius = radius * randomFactor;
    
    lat = centerLat + (adjustedRadius/6371) * cos(angle) * (180/PI);
    lng = centerLng + (adjustedRadius/6371) * sin(angle) * (180/PI) / cos(centerLat);
}
// Замыкаем полигон: последняя точка = первая
```

**Почему неровный полигон лучше:**
- ✅ Выглядит естественнее (не идеальный круг)
- ✅ Меньше подозрений у модераторов
- ✅ Соответствует реальным зонам доставки

**Response:**
```json
{
  "success": true,
  "polygonId": "484317",  // ID созданного полигона
  "hash": ""
}
```

---

## 5. Привязка полигона к зоне

### POST `/api/delivery-polygon-service/area/update`

Привязывает созданный полигон к зоне доставки.

**Body:**
```json
{
  "area": {
    "id": "957509",                      // ID зоны
    "name": "Доставка 15 мин",           // Название зоны
    "delivery_time": "15",                // Время доставки
    "multi_polygon_ids": ["484317"]       // Массив ID полигонов
  }
}
```

**Response:**
```json
{
  "success": true
}
```

---

## 6. Привязка склада к методу доставки

### POST `/api/delivery-polygon-service/delivery-method/save/warehouse`

Привязывает склад к методу доставки с указанием координат склада.

**Body:**
```json
{
  "delivery_method_id": 14650466,     // ID метода (число)
  "warehouse_id": "4249339",          // ID черновика склада (строка)
  "warehouse_location": {
    "lat": 59.376881,                 // Широта склада
    "long": 28.212974                 // Долгота склада
  }
}
```

**Response:**
```json
{
  "success": true
}
```

---

## 7. Настройка возвратов

### POST `/api/seller-returns-methods/v1/returns-setting`

Настраивает инструкции по возврату товаров.

**Body:**
```json
{
  "delivery_method_id": 14650466,
  "courier_instruction": {
    "comment": "",                    // Комментарий для курьера
    "contact_days": 1,                // Дней на связь
    "used_warehouse_phone": true      // Использовать телефон склада
  }
}
```

**Response:** пустой (status 200)

---

## 8. Установка расписания курьера (ОБЯЗАТЕЛЬНО!)

### POST `/api/delivery-method-service/delivery-method/courier-schedule/set`

⚠️ **КРИТИЧНО:** Без этого шага активация вернёт ошибку 400:
> "Для экспресс доставки должно быть указано рабочие расписание"

Устанавливает расписание работы курьера для экспресс-доставки.

**Body:**
```json
{
  "company_id": 2722344,              // ID компании
  "is_same_as_warehouse": true,       // Использовать расписание склада
  "delivery_method_id": 14650466      // ID метода доставки
}
```

**Альтернативный вариант** (своё расписание):
```json
{
  "company_id": 2722344,
  "is_same_as_warehouse": false,
  "delivery_method_id": 14650466,
  "working_days": [
    { "day": 1, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 2, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 3, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 4, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 5, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 6, "schedule": { "from": "09:00", "to": "21:00" } },
    { "day": 7, "schedule": { "from": "09:00", "to": "21:00" } }
  ]
}
```

**Response:**
```json
{
  "result": true
}
```

---

## 9. Активация метода доставки

### POST `/api/delivery-method-service/delivery-method/activate`

Активирует метод доставки и создает финальный склад.

**Body:**
```json
{
  "company_id": 2722344,              // ID компании
  "delivery_method_id": 14650466      // ID метода доставки
}
```

**Response:**
```json
{
  "result": true,
  "warehouse_id": 1020005003317990    // ID созданного склада (финальный!)
}
```

---

## Вспомогательные API

### Получение информации о зонах

#### POST `/api/delivery-polygon-service/area/by-delivery-method`

```json
// Body:
{"delivery_method_id": 14650466}

// Response:
{
  "areas": [
    {
      "id": "957509",
      "name": "Область 2",
      "deliveryMethodId": "14650466",
      "multiPolygonIds": ["484317"],
      "deliveryTime": "15"
    }
  ]
}
```

### Получение полигонов по ID

#### POST `/api/delivery-polygon-service/v2/polygon/by-ids`

```json
// Body:
{"ids": ["484317"]}

// Response:
{
  "polygons": [
    {
      "id": "484317",
      "coordinates": "[[[...координаты...]]]",
      "createdAt": "2025-12-25T20:01:32.851917Z",
      "updatedAt": "2025-12-25T20:01:32.851917Z",
      "isBase": false
    }
  ]
}
```

### Проверка метода доставки

#### GET `/api/delivery-polygon-service/delivery-method/check?id={id}`

```json
// Response:
{"isExist": true}
```

### Получение информации о методе доставки

#### GET `/api/delivery-polygon-service/delivery-method/by-id?id={id}`

```json
// Response:
{
  "deliveryMethodInfo": {
    "id": 14650466,
    "isAggregator": false,
    "warehouseId": "4249339",
    "isActive": false,
    "warehousePolygonId": "484317",
    "type": "self",
    "selfMethodSpecial": {
      "selfRadius": 0,
      "techPolygonId": "0",
      "withNonBasePolygons": true
    }
  }
}
```

### Расписание курьеров

#### POST `/api/delivery-method-service/v2/delivery-method/courier-schedule/get`

```json
// Body:
{
  "company_id": 2722344,
  "delivery_method_id": 14650466
}

// Response:
{
  "result": {
    "delivery_method_id": 14650466,
    "warehouse_id": 0,
    "company_id": 2722344,
    "working_days": [
      {"day": 1, "schedule": {"from": "09:00", "to": "21:00"}},
      {"day": 2, "schedule": {"from": "09:00", "to": "21:00"}},
      // ... дни 3-7
    ],
    "is_same_as_warehouse": true
  }
}
```

### Получение информации о складе

#### POST `/api/site/logistic-service/facade/warehouse/get`

```json
// Body:
{
  "companyId": 2722344,
  "warehouseId": 1020005003317990
}

// Response:
{
  "result": {
    "warehouse": {
      "warehouseId": 1020005003317990,
      "companyId": 2722344,
      "name": "Сельпо",
      "address": {
        "country": "Россия",
        "region": "Приморский",
        "city": "Уссурийск",
        "street": "Комсомольская улица",
        "house": "30",
        "zipcode": "692512",
        "latitude": 43.802949,
        "longitude": 131.944248,
        "utc": "UTC+10:00"
      },
      "status": "created",
      "workingDays": [1,2,3,4,5,6,7],
      "warehouseType": "rfbs_express",
      "phone": "+7 966 879 96 66"
    }
  }
}
```

### Список складов

#### POST `/api/site/logistic-service/v5/facade/warehouse/list`

```json
// Body:
{
  "cursor": "",
  "companyId": 2722344,
  "search": "",
  "statusFilter": 1,
  "warehousesLimit": 50
}
```

---

## Константы и идентификаторы

| Параметр | Значение | Описание |
|----------|----------|----------|
| `companyId` | 2722344 | ID компании продавца |
| `warehouseType` | `rfbs_express` | Тип склада Express |
| `delivery_type` | `self` | Самостоятельная доставка |
| `deliveryTypeId` | 1 | ID типа доставки |

---

## Важные заметки

### Формат координат полигона

Координаты должны быть:
- В формате `[широта, долгота]` (обратите внимание - lat первый!)
- Полигон замкнутый (первая точка = последняя)
- Обернуты в тройной массив `[[[...]]]`
- Переданы как JSON-строка

### Последовательность действий

1. Сначала создается **draft warehouse** (черновик склада) через UI или API
2. Затем создается **delivery method** (метод доставки) 
3. После этого можно создавать **area** (зону) и **polygon** (полигон)
4. Полигон привязывается к зоне через `area/update`
5. Склад привязывается к методу через `delivery-method/save/warehouse`
6. Метод активируется через `delivery-method/activate`

### Типы контента

Все запросы используют:
- `Content-Type: application/json`
- `Accept: application/json`
- Авторизация: cookies (автоматически при открытой сессии)

---

## Скрипты автоматизации

| Файл | Описание | Режим |
|------|----------|-------|
| `create_warehouse_full_auto.js` | **Полная автоматизация** — указываем только ID и адрес | 🤖 Авто |
| `create_warehouse_api.js` | Пошаговый скрипт с ручным контролем | 🔧 Ручной |

### create_warehouse_full_auto.js

**Одна команда для полного создания:**
```javascript
createWarehouseFully()
```

**Если черновик создан вручную:**
```javascript
continueFromDraft("warehouseDraftId")
continueFromDraft("draftId", "methodId")
```

### Параметры CONFIG

```javascript
const CONFIG = {
    companyId: 2722344,                  // ID компании (обязательно)
    warehouseAddress: "полный адрес",    // Адрес склада (обязательно)
    warehouseName: "",                   // Авто из города если пусто
    warehousePhone: "",                  // Авто генерация если пусто
    deliveryTimeMinutes: 15,             // Время доставки
    courierSpeedKmh: 30,                 // Скорость для расчета радиуса
    workingDays: [1,2,3,4,5,6,7],        // 1=Пн, 7=Вс
    workingHours: {from:"09:00", to:"21:00"}
};
```

---

## Формулы расчета

### Радиус доставки из времени

```
radius_km = (speed_kmh × minutes / 60) × 0.7
```

| Время | Скорость | Радиус |
|-------|----------|--------|
| 15 мин | 30 км/ч | ~5.25 км |
| 30 мин | 30 км/ч | ~10.5 км |
| 60 мин | 30 км/ч | ~21 км |

**Коэффициент 0.7** — поправка на реальные маршруты (не по прямой).

### Генерация круглого полигона

24 точки по окружности с центром в координатах склада:
```
точка[i] = центр + радиус × (cos θ, sin θ / cos(lat))
где θ = 2π × i / 24
```

---

# Другие API модулей Ozon Seller Toolbox

---

## 🔄 WORKFLOW: Добавление товаров (ProductsModule)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  • company_id (из cookies: sc_company_id)                               │
│  • searchQuery (название товара для поиска)                             │
│  • price (цена товара)                                                  │
│  • offer_id (артикул продавца)                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 1: ПОИСК ТОВАРОВ                                                   │
│  POST /api/v1/search-variant-model                                      │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  name (поисковый запрос), limit: 10                             │
│  OUTPUT: items[] → список вариантов                                     │
│                                                                         │
│  Каждый item содержит:                                                  │
│    • variant_id — ID варианта для добавления                            │
│    • name — название товара                                             │
│    • attributes — статус доступности (12085: allow/deny)                │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ items[0].variant_id
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 2: ДОБАВЛЕНИЕ ТОВАРА                                               │
│  POST /api/v1/item/create-by-variant                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  variant_id, offer_id, price, company_id, vat, currency         │
│  OUTPUT: result.item_id — ID добавленного товара                        │
│                                                                         │
│  ⚠️ Фильтр: attributes[12085] !== "deny"                                │
│  ⚠️ Задержка: 300-500 мс между добавлениями                             │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЗУЛЬТАТ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  • item_id — ID товара в вашем магазине                                 │
│  • Товар появится в списке товаров через 1-5 минут                      │
└─────────────────────────────────────────────────────────────────────────┘
```

### Граф данных ProductsModule:

```
searchQuery ──► search-variant-model ──► items[]
                                            │
                                    variant_id ──┐
                                                 │
price, offer_id, company_id ─────────────────────┴──► item/create-by-variant
                                                            │
                                                            ▼
                                                        item_id
```

---

## 🔄 WORKFLOW: Изменение цен (PriceChangerModule)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  • company_id (из cookies: sc_company_id)                               │
│  • newPrice (новая цена)                                                │
│  • user_name (email пользователя)                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 1: ПОЛУЧЕНИЕ СПИСКА ТОВАРОВ                                        │
│  POST /api/v1/products/list-by-filter                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, limit: 100, cursor (для пагинации)                 │
│          aggregate.parts: ["PART_ITEM", "PART_PRICE", "PART_STATUS"]    │
│  OUTPUT: products[] → список всех товаров с ценами                      │
│                                                                         │
│  Каждый product содержит:                                               │
│    • item_id — ID товара                                                │
│    • part_item.name — название                                          │
│    • part_price.price.units — текущая цена                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ products[].item_id
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 2: МАССОВОЕ ИЗМЕНЕНИЕ ЦЕН                                          │
│  POST /api/seller-price-api/v1/price-batch-set                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  prices[] (массив { item_id, price })                           │
│          company_id, user_name, currency, source                        │
│  OUTPUT: result.updated — количество обновлённых                        │
│                                                                         │
│  ⚠️ Рекомендация: максимум 100 товаров за запрос                        │
│  ⚠️ Задержка: 5-10 сек между батчами                                    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЗУЛЬТАТ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  • updated — количество товаров с обновлёнными ценами                   │
│  • Изменения отобразятся в личном кабинете через 1-2 минуты             │
└─────────────────────────────────────────────────────────────────────────┘
```

### Граф данных PriceChangerModule:

```
company_id ──► list-by-filter ──► products[]
                                      │
                                  item_id[]
                                      │
newPrice, user_name ──────────────────┴──► price-batch-set
                                                  │
                                                  ▼
                                           updated count
```

---

## 🔄 WORKFLOW: Рекламная кампания (PromotionModule)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  • company_id (из cookies: sc_company_id)                               │
│  • campaignName (название кампании)                                     │
│  • activeFrom (дата начала, DD.MM.YYYY)                                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  СОЗДАНИЕ КАМПАНИИ                                                      │
│  POST /api/sc/v4/create-promotion                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  HEADERS: x-o3-page-type: ReviewsPromotions                             │
│  INPUT:  scCompanyId, name, activeFrom, companyType                     │
│  OUTPUT: promotionId, promotionUuid                                     │
│                                                                         │
│  ⚠️ Специальный заголовок x-o3-page-type: ReviewsPromotions             │
│  ⚠️ Формат даты: DD.MM.YYYY                                             │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЗУЛЬТАТ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  • promotionId — ID кампании (числовой)                                 │
│  • promotionUuid — UUID кампании                                        │
│  • Кампания появится в разделе "Отзывы и вопросы"                       │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Товары (ProductsModule)

### POST `/api/v1/search-variant-model`

Поиск товаров по названию для добавления в магазин.

**Headers:** стандартные x-o3-* (см. выше)

**Body:**
```json
{
  "name": "губка для посуды",
  "limit": "10",
  "last_id": null
}
```

**Response:**
```json
{
  "items": [
    {
      "variant_id": 123456789,
      "name": "Губка для посуды 5 шт",
      "attributes": [
        { "key": "12085", "value": "allow" }
      ]
    }
  ],
  "last_id": "abc123"
}
```

**Примечания:**
- `last_id` — для пагинации (передать в следующий запрос)
- `attributes.12085 = "deny"` — товар недоступен для добавления

---

### POST `/api/v1/item/create-by-variant`

Добавление товара в магазин по variant_id.

**Body:**
```json
{
  "variant_id": 123456789,
  "offer_id": "12345",
  "price": "3100",
  "vat": 0,
  "company_id": "2721175",
  "currency": "RUB"
}
```

**Response:**
```json
{
  "result": {
    "item_id": 987654321
  }
}
```

---

## Цены (PriceChangerModule)

### POST `/api/v1/products/list-by-filter`

Получение списка товаров с ценами для фильтрации и изменения.

**Body:**
```json
{
  "aggregate": {
    "parts": ["PART_ITEM", "PART_PRICE", "PART_STATUS"],
    "human_texts": true
  },
  "filters": {
    "price_color_indexes": [],
    "search": "",
    "categories": []
  },
  "visibility": "ALL",
  "sort_by": "SORT_BY_CREATED_AT",
  "sort_dir": "SORT_DIRECTION_DESC",
  "company_id": "2721175",
  "limit": 100,
  "cursor": "",
  "return_total_items": true
}
```

**Response:**
```json
{
  "products": [
    {
      "item_id": 987654321,
      "part_item": {
        "name": "Губка для посуды",
        "sku": "123456"
      },
      "part_price": {
        "price": { "units": "3100" }
      }
    }
  ],
  "cursor": "next_page_token"
}
```

---

### POST `/api/seller-price-api/v1/price-batch-set`

Массовое изменение цен товаров.

**Body:**
```json
{
  "prices": [
    {
      "item_id": "987654321",
      "min_auto_price": "0",
      "old_price": "0",
      "price": "35",
      "net_price": "0",
      "vat": "0",
      "metadata": {
        "manage_elastic_boosting_through_price": true
      }
    }
  ],
  "currency": "RUB",
  "company_id": "2721175",
  "user_name": "user@email.com",
  "source": "api_price_set_v2"
}
```

**Response:**
```json
{
  "result": {
    "updated": 1,
    "errors": []
  }
}
```

**Примечания:**
- `user_name` — email пользователя (для аудита)
- Рекомендуемая задержка: 5-10 сек между изменениями

---

## Рекламные кампании (PromotionModule)

### POST `/api/sc/v4/create-promotion`

Создание рекламной кампании для сбора отзывов.

**Headers:**
```http
x-o3-page-type: ReviewsPromotions
```

**Body:**
```json
{
  "scCompanyId": "2721175",
  "companyType": "COMPANY_TYPE_SELLER",
  "name": "Кампания отзывов 27.12.2025",
  "isAutogenerated": false,
  "withDetailedReview": false,
  "withRatingReview": false,
  "promotionId": "",
  "activeFrom": "27.12.2025"
}
```

**Response:**
```json
{
  "promotionId": "12345",
  "promotionUuid": "abc-def-123-456"
}
```

**Примечания:**
- `activeFrom` — формат `DD.MM.YYYY`
- `x-o3-page-type` должен быть `ReviewsPromotions`

---

## SKU (SKUModule)

Использует тот же endpoint `/api/v1/products/list-by-filter` что и PriceChangerModule, но запрашивает только `PART_ITEM` для получения SKU всех товаров.

---

## Остатки (StockModule)

### 🔄 WORKFLOW: Установка остатков

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ВХОДНЫЕ ДАННЫЕ                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  • company_id (из cookies: sc_company_id)                               │
│  • minStock (минимальный остаток, по умолчанию 10)                      │
│  • maxStock (максимальный остаток, по умолчанию 50)                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 1: ПОЛУЧЕНИЕ СКЛАДОВ                                               │
│  POST /api/site/logistic-service/v2/warehouse/list/short                │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, status_not_in: ["disabled"]                        │
│  OUTPUT: result[] → список складов                                      │
│                                                                         │
│  Фильтр: is_rfbs = true (только RFBS склады)                            │
│  Берём первый RFBS склад → warehouse_id                                 │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                          warehouse_id
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 2: ПОЛУЧЕНИЕ ТОВАРОВ                                               │
│  POST /api/v1/products/list-by-filter                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, limit: 100, cursor (пагинация)                     │
│  OUTPUT: products[] → offer_id для каждого товара                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                          offer_id[]
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ШАГ 3: УСТАНОВКА ОСТАТКОВ                                              │
│  POST /api/site/item-stock-service/rfbs/item/stock/batch-set            │
├─────────────────────────────────────────────────────────────────────────┤
│  INPUT:  company_id, stocks[] (батчи по 50 товаров)                     │
│          Каждый stock: { offer_id, stock: random(min,max), warehouse_id }│
│  OUTPUT: status[] → updated: true/false для каждого товара              │
│                                                                         │
│  ⚠️ Батчи по 50 товаров с задержкой 500 мс                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         РЕЗУЛЬТАТ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│  • updated — количество товаров с обновлёнными остатками                │
│  • Остатки отобразятся в личном кабинете через 1-2 минуты               │
└─────────────────────────────────────────────────────────────────────────┘
```

### POST `/api/site/logistic-service/v2/warehouse/list/short`

Получение списка складов компании.

**Body:**
```json
{
  "company_id": 2721059,
  "status_not_in": ["disabled"]
}
```

**Response:**
```json
{
  "result": [
    {
      "warehouse_id": 1020005003426190,
      "company_id": 2721059,
      "name": "Городской 1",
      "status": "created",
      "is_rfbs": true,
      "is_crossborder": false,
      "is_fbp": false
    }
  ],
  "cursor": "...",
  "has_next": false
}
```

---

### POST `/api/site/item-stock-service/rfbs/item/stock/batch-set`

Массовая установка остатков товаров на складе.

**Body:**
```json
{
  "company_id": 2721059,
  "stocks": [
    {
      "offer_id": "65990",
      "stock": 20,
      "warehouse_id": 1020005003426190
    },
    {
      "offer_id": "65991",
      "stock": 35,
      "warehouse_id": 1020005003426190
    }
  ]
}
```

**Response:**
```json
{
  "status": [
    {
      "warehouse_id": 1020005003426190,
      "offer_id": "65990",
      "updated": true,
      "errors": [],
      "item_id": 3309166317,
      "sku": 3295499699,
      "size": 1
    }
  ]
}
```

**Примечания:**
- `offer_id` — артикул продавца (строка)
- `stock` — количество (число)
- `warehouse_id` — ID склада RFBS
- Рекомендуемый батч: 50 товаров
- Задержка между батчами: 500 мс

---

# Общие рекомендации

## Задержки между запросами

| Операция | Рекомендуемая задержка |
|----------|------------------------|
| Поиск товаров | 300 мс |
| Добавление товара | 500 мс |
| Изменение цены | 5-10 сек |
| Создание склада | 2-3 сек между шагами |
| Установка остатков | 500 мс между батчами |

## Обработка ошибок

| HTTP код | Причина | Решение |
|----------|---------|---------|
| 403 | Отсутствуют заголовки x-o3-* | Добавить все обязательные заголовки |
| 403 | Истёкла сессия | Обновить страницу, войти заново |
| 400 | Неверный формат body | Проверить JSON структуру |
| 429 | Rate limit | Увеличить задержки |

## Лимиты

- Максимум 100 товаров за запрос list-by-filter
- Максимум 50 товаров за запрос batch-set (остатки)
