# 🔧 Tools - Утилиты и вспомогательные инструменты

Вспомогательные скрипты и утилиты.

## 📋 Файлы

| Файл | Описание |
|------|----------|
| `ozon_toolbox.user.js` | 🧰 **Главный Tampermonkey скрипт** - всё в одном (v3.8) |
| `interceptor.js` | 🔍 Перехватчик API запросов (сохраняет в localStorage) |
| `articul.js` | Работа с артикулами товаров |
| `bridge_server.js` | Мост для внешних HTTP запросов |
| `debug_scan_page.js` | Отладка и сканирование страниц |

---

## 🧰 Ozon Toolbox (Tampermonkey)

**Версия:** 3.8  
**Файл:** `ozon_toolbox.user.js`

Комплексный Tampermonkey скрипт для автоматизации работы с Ozon Seller. Включает виджет с вкладками для различных операций.

### Установка

1. Установи [Tampermonkey](https://www.tampermonkey.net/)
2. Создай новый скрипт и вставь код из `ozon_toolbox.user.js`
3. Открой https://seller.ozon.ru — виджет появится в правом нижнем углу

### Модули

#### 📦 ProductsModule — Добавление товаров
Массовое добавление товаров по поисковому запросу.

```javascript
// Настройки
{
  searchQuery: "губка",    // Поисковый запрос
  limit: 10,               // Товаров на страницу
  maxPages: 20,            // Макс. страниц
  price: "3100",           // Цена товара
  maxToAdd: 9              // Макс. товаров для добавления
}
```

#### 🏭 WarehouseModule — Создание складов
Автоматическое создание складов с настройками доставки.

```javascript
{
  warehouseAddress: "",           // Адрес склада
  warehouseName: "",              // Название
  warehousePhone: "",             // Телефон
  deliveryTimeMinutes: 15,        // Время доставки
  courierSpeedKmh: 30,            // Скорость курьера
  workingHoursFrom: "08:00",      // Начало работы
  workingHoursTo: "22:00",        // Конец работы
  speedMode: "human"              // Режим: human/fast
}
```

#### 💰 PriceChangerModule — Массовое изменение цен
Изменяет цены товаров выше заданного порога на случайное значение в диапазоне.

```javascript
{
  minThreshold: 100,    // Порог (меняются цены ВЫШЕ)
  newPriceMin: 27,      // Новая цена от
  newPriceMax: 50,      // Новая цена до
  userEmail: ''         // Email продавца для API
}
```

#### 📋 SKUModule — Экспорт SKU
Загружает список всех SKU товаров магазина через API.

```javascript
SKUModule.run();              // Загрузить SKU
SKUModule.copyToClipboard();  // Скопировать в буфер
SKUModule.clear();            // Очистить список
```

#### 📢 PromotionModule — Рекламные кампании
Создание рекламных кампаний через API `/api/sc/v4/create-promotion`.

```javascript
PromotionModule.run("Название кампании");
```

**Параметры кампании:**
- `companyType`: "promo" (для отзывов)
- `isAutogenerated`: false
- `withDetailedReview`: true
- `withRatingReview`: false

#### 🔔 NotificationSystem — Уведомления
Система всплывающих уведомлений с тёмной темой.

```javascript
NotificationSystem.show("Сообщение", "info");     // Типы: info, success, error
NotificationSystem.success("Готово!");
NotificationSystem.error("Ошибка!");
```

### 🔍 API Перехватчик

Перехватывает все fetch запросы к `/api/` и сохраняет в localStorage.

**Кнопки:**
- **Запись/Пауза** — управление записью
- **Консоль** — показать логи в DevTools
- **Скачать** — скачать JSON файл
- **Копировать** — в буфер обмена
- **Очистить** — удалить все запросы

**Данные запроса:**
```javascript
{
  timestamp: "2025-12-27T...",
  url: "https://seller.ozon.ru/api/...",
  method: "POST",
  headers: {...},
  cookies: {...},
  body: {...},
  response: {...},
  status: 200
}
```

### 📤 Экспорт сессии (v3.8)

Новый раздел во вкладке "API" для экспорта данных сессии.

**Три кнопки:**
| Кнопка | Действие |
|--------|----------|
| **JSON** | Скачивает `ozon_session_ДАТА.json` с полной информацией |
| **curl** | Копирует готовую curl команду для тестирования API |
| **Python** | Копирует Python код с requests |

**Кнопка справки:** ❓ Как получить ВСЕ cookies

#### Данные сессии (JSON)

```javascript
{
  company_id: "12345",
  timestamp: "2025-12-27T...",
  cookies: { sc_company_id: "...", ... },
  cookies_string: "sc_company_id=...; ...",
  user_agent: "Mozilla/5.0...",
  headers: {
    "Accept": "application/json",
    "Content-Type": "application/json",
    "x-o3-app-name": "seller-ui",
    "x-o3-language": "ru",
    "x-o3-company-id": "12345"
  },
  rfuid: "...",  // fingerprint если есть
  test_url: "https://seller.ozon.ru/api/v2/company/finance-info",
  curl_command: "curl -X POST...",
  python_code: "import requests...",
  note: "ВАЖНО: HttpOnly cookies недоступны через JS"
}
```

#### ⚠️ Важно: HttpOnly cookies

JavaScript **не может** получить HttpOnly cookies — это защита браузера. Но они нужны для авторизации в API.

**3 способа получить ВСЕ cookies:**

1. **DevTools → Application → Cookies**
   - F12 → Application → Cookies → seller.ozon.ru
   - Ctrl+A → Ctrl+C → преобразовать в формат `name=value; name2=value2`

2. **DevTools → Network → Request Headers** (проще)
   - F12 → Network → обнови страницу
   - Найди запрос к `/api/` → Headers → Cookie
   - Скопируй всё значение

3. **Copy as cURL** (самый простой)
   - F12 → Network → правый клик на запрос
   - Copy → Copy as cURL (bash)
   - Готовая команда со ВСЕМИ headers!

### 🎮 Консольные команды

```javascript
// Модули
window.OzonToolbox.ProductsModule.run(config);
window.OzonToolbox.WarehouseModule.run(config);
window.OzonToolbox.PriceChangerModule.run(config);
window.OzonToolbox.SKUModule.run();
window.OzonToolbox.PromotionModule.run("Название");

// Данные
window.OzonToolbox.getCompanyId();     // ID компании
window.OzonToolbox.getConfig();        // Текущие настройки
window.OzonToolbox.getRequests();      // Перехваченные запросы

// Экспорт сессии (v3.8)
window.OzonToolbox.session.getCookies();        // Cookies как объект
window.OzonToolbox.session.getCookiesString();  // Cookies как строка
window.OzonToolbox.session.getSessionData();    // Полные данные сессии
window.OzonToolbox.session.generateCurl();      // curl команда
window.OzonToolbox.session.generatePython();    // Python код
window.OzonToolbox.session.showHelp();          // Справка по cookies

// Отладка
window.OzonToolbox.debug.isEnabled();              // Статус отладки
window.OzonToolbox.debug.setEnabled(true/false);   // Вкл/выкл отладку
window.OzonToolbox.debug.dumpLastRequests(20);     // Последние N запросов
```

### 📝 История версий

| Версия | Изменения |
|--------|-----------|
| 3.8 | Экспорт сессии (JSON/curl/Python), справка по HttpOnly cookies |
| 3.7 | PromotionModule — создание рекламных кампаний |
| 3.6 | PriceChangerModule, SKUModule, NotificationSystem |
| 3.5 | API перехватчик с cookies и headers |
| 3.0 | Виджет с вкладками, ProductsModule, WarehouseModule |

---

## 🔍 Interceptor

Перехватывает все fetch/XHR запросы и сохраняет в localStorage.

```javascript
// После вставки в консоль доступны команды:
showRequests()     // показать все запросы
showAPIRequests()  // только API запросы
copyRequests()     // скопировать в буфер
clearRequests()    // очистить
getRequests()      // получить массив
```

## 🌉 Bridge Server

Позволяет отправлять запросы из внешних приложений в контекст браузера.

```javascript
// Запуск моста:
// Вставить код bridge_server.js в консоль
```

## 🐛 Debug Scan Page

Инструмент для отладки и анализа структуры страниц Ozon.

```javascript
// Использование:
scanPage();  // Выводит структуру страницы
```
