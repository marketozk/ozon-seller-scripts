# 🔍 Руководство по перехватчику запросов

## Описание

**Перехватчик** (`interceptor.js`) — это скрипт для браузерной консоли, который записывает все HTTP-запросы (fetch и XMLHttpRequest), отправляемые страницей. 

Используется для анализа API Ozon Seller и создания автоматизированных скриптов.

---

## 📋 Что перехватывается

Для каждого запроса сохраняется:

| Поле | Описание | Пример |
|------|----------|--------|
| `timestamp` | Время запроса | `2025-12-25T14:30:00.000Z` |
| `url` | URL адрес API | `https://seller.ozon.ru/api/v1/warehouse/create` |
| `method` | HTTP метод | `POST`, `GET`, `PUT` |
| `headers` | Заголовки запроса | `{ "x-o3-company-id": "123456", ... }` |
| `body` | Тело запроса (данные) | `{ "name": "Мой склад", ... }` |
| `response` | Ответ сервера | `{ "id": "789", "success": true }` |
| `status` | HTTP статус код | `200`, `403`, `500` |

---

## 🚀 Как использовать

### Шаг 1: Активация перехватчика

1. Открой нужную страницу на `seller.ozon.ru`
2. Открой **DevTools** (F12) → вкладка **Console**
3. Скопируй весь код из файла `interceptor.js`
4. Вставь в консоль и нажми **Enter**
5. Увидишь сообщение:
   ```
   ✅ ПЕРЕХВАТЧИК ЗАПРОСОВ АКТИВИРОВАН!
   💾 Данные сохраняются в localStorage (не теряются!)
   ```

### Шаг 2: Выполнение действий

Теперь выполняй нужные действия на сайте:
- Создание склада
- Добавление товара
- Настройка доставки
- и т.д.

Все запросы будут логироваться в консоль:
```
🔵 FETCH: POST https://seller.ozon.ru/api/v1/warehouse/create
   📤 Body: { name: "Склад", address: "..." }
   📥 Response: { id: "123", success: true }
```

### Шаг 3: Работа с данными

После выполнения действий используй команды:

```javascript
showRequests()      // Показать все перехваченные запросы
showAPIRequests()   // Показать только API запросы Ozon (фильтр)
copyRequests()      // Скопировать все запросы в буфер обмена
clearRequests()     // Очистить список запросов
getRequests()       // Получить массив запросов для работы в коде
```

---

## 💾 Сохранение данных

### Особенность: localStorage

Данные сохраняются в `localStorage` браузера, поэтому:
- ✅ **Не теряются при перезагрузке страницы**
- ✅ При повторной вставке перехватчика — старые данные подхватываются
- ⚠️ Очищаются при вызове `clearRequests()` или очистке данных браузера

### Экспорт в файл

Для сохранения в файл выполни в консоли:

```javascript
// Скачает файл requests.json
(function() {
  const data = localStorage.getItem('_interceptedRequests') || '[]';
  const blob = new Blob([data], {type: 'application/json'});
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'requests.json';
  a.click();
})();
```

---

## 📊 Примеры использования

### Пример 1: Перехват создания склада

1. Активируй перехватчик
2. Перейди на https://seller.ozon.ru/app/warehouse/create
3. Заполни форму склада и сохрани
4. Выполни `showAPIRequests()` — увидишь все API вызовы

### Пример 2: Фильтрация нужных запросов

```javascript
// Получить только POST запросы к warehouse API
const requests = getRequests();
const warehouseRequests = requests.filter(r => 
  r.method === 'POST' && r.url.includes('warehouse')
);
console.log(warehouseRequests);
```

### Пример 3: Найти нужный endpoint

```javascript
// Поиск по URL
const requests = getRequests();
requests.forEach((r, i) => {
  if (r.url.includes('api/v1')) {
    console.log(`[${i}] ${r.method} ${r.url}`);
  }
});
```

---

## 🔧 Передача данных для анализа

### Вариант 1: Вставить в чат (до ~50 запросов)

```javascript
copyRequests()  // Ctrl+V в чат
```

### Вариант 2: Сохранить в файл проекта

1. Выполни `copyRequests()`
2. Создай файл `requests.json` в папке проекта
3. Вставь содержимое
4. Copilot автоматически увидит файл

### Вариант 3: Скачать и перетащить

Используй скрипт экспорта (см. выше) → файл скачается → перетащи в VS Code

---

## ⚠️ Важные замечания

1. **Перехватчик нужно вставлять заново** после полного закрытия вкладки браузера (localStorage сохранится, но функции fetch перезатрутся)

2. **Очищай перед новой сессией** — `clearRequests()` чтобы не смешивать данные

3. **Большие ответы** могут обрезаться — для детального анализа используй вкладку Network в DevTools

4. **Заголовки авторизации** — перехватываются, но куки могут не отображаться (HttpOnly)

---

## 📁 Связанные файлы

| Файл | Описание |
|------|----------|
| `interceptor.js` | Код перехватчика |
| `search_ozon_api.js` | Скрипт поиска и добавления товаров (создан на основе перехвата) |
| `seller_product_add.js` | Массовое добавление товаров по URL |

---

## 🎯 Типичный workflow

```
1. clearRequests()           — очистить старые данные
2. [Вставить interceptor.js] — активировать перехват
3. [Выполнить действия]      — создать склад/товар/etc
4. showAPIRequests()         — посмотреть что записалось
5. copyRequests()            — скопировать для анализа
6. [Передать в Copilot]      — получить готовый скрипт
```
